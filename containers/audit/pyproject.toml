[project]
name = "dcont-audit"
version = "0.1.0"
description = "mitmproxy addons for dcont egress audit"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
